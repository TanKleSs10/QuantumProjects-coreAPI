/**
 * Barrel file for use case exports. Add concrete use case implementations
 * that orchestrate domain logic and coordinate with repositories here.
 */
export type PlaceholderUseCase = () => Promise<void>;
