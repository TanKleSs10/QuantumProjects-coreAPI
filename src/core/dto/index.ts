/**
 * Re-export DTO definitions from this barrel file as they are introduced.
 * DTOs translate between external inputs/outputs and the domain model.
 */
export type PlaceholderDTO = Record<string, never>;
