/**
 * Export cross-cutting utility helpers from this module.
 * Keep utilities pure and framework agnostic.
 */
export const noop = (): void => {
  // TODO: Replace with real utilities as the project grows.
};
